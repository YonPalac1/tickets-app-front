import {
    GET_ALL
  } from "../Constants";
  
  const dataInicial = {
    array: [],
  };
  export const reducerData = (state = dataInicial, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };